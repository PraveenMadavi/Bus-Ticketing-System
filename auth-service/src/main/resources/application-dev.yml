#AUTH-SERVICE-DEV
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/btsusers
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


jwt:
  key-id: auth-service-key