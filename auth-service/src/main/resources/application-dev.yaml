#AUTH-SERVICE-DEV

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/btsusers
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


jwt:
  key-id: auth-service-key

logging:
  level:
    org.springframework.security: DEBUG